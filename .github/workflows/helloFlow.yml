name: Hello Flow
on:
  workflow_dispatch:     
  push:                  
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Diga oi (echo)
        run: echo "Hello World!"

      - name: Diga valeu
        run: echo "Valeu - bye!"
